### **Деревья (Trees) — структура данных**  

Дерево — это иерархическая структура данных, состоящая из узлов (вершин), связанных между собой рёбрами. Оно широко применяется в программировании для представления данных с вложенными отношениями, таких как файловые системы, иерархии объектов, синтаксические деревья и многое другое.  

---

## **1. Основные понятия**  
- **Корень (Root)** — начальный узел дерева (не имеет родителя).  
- **Узел (Node)** — элемент дерева, содержащий данные и ссылки на дочерние узлы.  
- **Лист (Leaf)** — узел без дочерних элементов.  
- **Родитель (Parent)** и **потомок (Child)** — если узел `A` ссылается на `B`, то `A` — родитель, `B` — потомок.  
- **Глубина (Depth)** — длина пути от корня до узла.  
- **Высота (Height)** — максимальная глубина листьев дерева.  
- **Поддерево (Subtree)** — часть дерева, которую можно рассматривать как отдельное дерево.  

---

## **2. Виды деревьев**  

### **① Бинарное дерево (Binary Tree)**  
Каждый узел имеет **не более двух потомков** (левый и правый).  

**Пример:**  
```
        A
       / \
      B   C
     / \   \
    D   E   F
```
**Применение:**  
- Бинарные деревья поиска (BST).  
- Деревья выражений (парсинг математических формул).  

---

### **② Бинарное дерево поиска (BST — Binary Search Tree)**  
Частный случай бинарного дерева, где:  
- Левый потомок **меньше** родителя.  
- Правый потомок **больше** родителя.  

**Пример:**  
```
        5
       / \
      3   7
     / \   \
    2   4   8
```
**Применение:**  
- Эффективный поиск (`O(log n)` в сбалансированном случае).  

---

### **③ АВЛ-дерево (AVL Tree)**  
Самобалансирующееся BST, где разница высот поддеревьев (**коэффициент баланса**) не превышает 1.  

**Пример:**  
```
        5
       / \
      3   7
     / \   \
    2   4   8
```
**Применение:**  
- Гарантирует `O(log n)` для операций вставки/удаления.  

---

### **④ Красно-чёрное дерево (Red-Black Tree)**  
Ещё один тип сбалансированного BST, где каждый узел имеет цвет (красный или чёрный) и соблюдаются правила:  
1. Корень всегда чёрный.  
2. Дети красного узла — чёрные.  
3. Все пути от узла к листьям содержат одинаковое число чёрных узлов.  

**Применение:**  
- Реализация `std::map` в C++, `TreeMap` в Java.  

---

### **⑤ B-дерево (B-Tree)**  
Дерево, где узел может иметь **много потомков** (используется в базах данных и файловых системах).  

**Пример (B-дерево порядка 3):**  
```
        [10, 20]
       /   |   \
[5,7]  [15]  [25,30]
```
**Применение:**  
- Индексы в СУБД (MySQL, PostgreSQL).  

---

### **⑥ Префиксное дерево (Trie)**  
Структура для хранения строк, где каждый узел представляет символ.  

**Пример (хранение слов "cat", "car"):**  
```
        (root)
        /    \
       c      ...
      / \
     a   a
    / \   \
   t   r   ...
```
**Применение:**  
- Автодополнение, проверка орфографии.  

---

## **3. Обход деревьев**  
- **Прямой (Pre-order):** Корень → Левый → Правый.  
- **Симметричный (In-order):** Левый → Корень → Правый (для BST даёт отсортированный порядок).  
- **Обратный (Post-order):** Левый → Правый → Корень.  
- **По уровням (BFS — Breadth-First Search):** Обход уровня за уровнем.  

---

## **4. Применение деревьев**  
✅ **Поиск данных** (BST, AVL, B-деревья).  
✅ **Сортировка** (куча, сортировка с помощью BST).  
✅ **Синтаксический анализ** (AST — Abstract Syntax Tree).  
✅ **Сжатие данных** (алгоритм Хаффмана).  
✅ **ИИ** (деревья решений).  

---

## **Заключение**  
Деревья — мощный инструмент для работы с иерархическими данными. Выбор конкретного типа зависит от задачи:  
- **BST** — для простого поиска.  
- **AVL/RB-Tree** — если нужна балансировка.  
- **B-Tree** — для работы с диском (СУБД).  
- **Trie** — для строковых операций.  

Если нужно что-то простое — бинарное дерево, если важна скорость — сбалансированные деревья, если строки — Trie. 🚀